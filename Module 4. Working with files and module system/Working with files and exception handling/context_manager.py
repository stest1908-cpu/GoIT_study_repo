# Модуль 4 | Робота з файлами та модульна система
# Тема: Менеджер контексту (Context Manager)
# Розглянуто: with open(), автоматичне закриття файлу, читання/запис через with, цикл for по файлу
# -----------------------------------------------

#! Менеджер контексту

'''
Застосунок може виконати багато операцій між відкриттям та закриттям файлу. В будь-якому місці може статися помилка та застосунок завершиться аварійно, не повернувши файловий дескриптор системі. Така поведінка, як вже згадувалося, небажана і може призводити до втрати даних.
'''

#Щоб уникнути цього, можна взяти блок коду, в якому відбувається робота з файлом, у блок try ... except:

fh = open('file.txt', 'w') # відкриваємо файл для запису
try:
    # Виконуємо операції з файлом
    fh.write('Some data')
finally:
    # Закриття файлу в блоці finally гарантує, що файл закриється навіть у разі помилки
    fh.close()

'''
Цей підхід гарантує, що файл закриється, але код виглядає трохи загромадженим. Якщо станеться виняток, то обов'язково виконається блок finally, в якому файл буде закритий. Такий підхід гарантує, що файловий дескриптор буде обов'язково повернений системі. Але такий підхід не надто елегантний та читабельний.
'''

'''
!!! ☝ Використання конструкції with є кращою практикою, оскільки вона гарантує, що файл буде належним чином закритий після завершення блоку коду.
'''

'''
Для покращення читабельності коду при збереженні функціоналу можна скористатися менеджером контексту with. Менеджер контексту в Python - це спосіб використання ресурсів, який автоматично забезпечує правильне закриття файлу, незалежно від того, чи виникла помилка чи ні. Це робить код не тільки більш читабельним, але й безпечнішим.
'''
with open('text_with.txt', 'w') as fh:
    # Виконання операцій з файлом
    fh.write('Some data')
# Файл автоматично закриється після виходу з блоку with

'''
Менеджер контексту складається з ключового слова with, після якого викликається сам менеджер і, якщо щось потрібно повернути з менеджера, то це щось можна передати у змінну, оголошену після ключового слова as. Далі ставиться двокрапка і блок коду, який буде виконаний всередині менеджера. У прикладі з try ... finally — це код, який йде всередині блоку try. Коли код виконається, менеджер контексту виконає те, що повинен зробити в будь-якому випадку — закрити файл, наприклад (це те, що відбувається у блоці finally).
'''

'''
Менеджер контексту open синтаксично повністю повторює свого класичного тезку open, вони повністю ідентичні у використанні.

З точки зору роботи цей приклад виконує у точності те саме, що і попередній з блоком try ... finally. Але замість п'яти рядків коду, ви можете написати два, і код виглядатиме читабельнішим. Тут with автоматично забезпечує закриття файлу, навіть якщо виникає помилка всередині блоку коду. Це робить код не тільки чистішим, але й безпечнішим, оскільки знижує ризик забути закрити файл.
'''

'''
Такий спосіб роботи з файлами є рекомендованим у Python, оскільки гарантує, що програміст не забуде закрити файл у будь-якому разі.

Перепишемо приклад, з попереднього розділу, але вже з менеджером контексту:
'''

with open("text.upd_with.txt", "w") as fh:
    fh.write("first line\nsecond line\nthird line")

with open("text.upd_with.txt", "r") as fh:
    lines = [el.strip() for el in fh.readlines()]

print(lines)

#Як бачимо коду стало менше, він чистіше, а виведення залишилось таким самим:
['first line', 'second line', 'third line']

#Надалі ми будемо використовувати тільки менеджер контексту при роботі з файлами.

#--------------------------------------------------#

#Task from Claude

'''
Завдання1: Щоденник

1. Створи файл diary.txt
2. Запиши в нього 3 рядки (будь-який текст, наприклад записи щоденника)
3. Відкрий файл і прочитай тільки перший рядок
4. Виведи його в консоль
5. Закрий файл
'''

# Створюємо файл diary.txt та записуємо в нього 3 рядки
with open("test_diary.txt", "w") as fh:
    fh.write("Today was a good day.\n")
    fh.write("I went for a walk in the park.\n")
    fh.write("I had a delicious dinner.\n")

# Відкриваємо файл для читання і прочитуємо перший рядок
with open("test_diary.txt", "r") as fh:
    first_line = fh.readline().strip()  # Читаємо перший рядок та видаляємо пробіли
    second_line = fh.readline().strip()  # Читаємо другий рядок та видаляємо пробіли

# Виводимо перший рядок
print(first_line)
# Виводимо другий рядок
print(second_line)

'''
Завдання2: Логування
1. Створи файл log.txt
2. Запиши в нього 5 рядків (наприклад, логування подій):
    - "2024-06-01 10:00:00 - Application started"
    - "2024-06-01 10:05:00 - User logged in"
    - "2024-06-01 10:10:00 - User performed an action"
    - "2024-06-01 10:15:00 - User logged out"
    - "2024-06-01 10:20:00 - Application stopped"
3. Відкрий файл і виведи всі рядки через цикл for
4. Все через with, без close()

'''

with open("log.txt", "w") as fh:
    fh.write("2024-06-01 10:00:00 - Application started\n")
    fh.write("2024-06-01 10:05:00 - User logged in\n")
    fh.write("2024-06-01 10:10:00 - User performed an action\n")
    fh.write("2024-06-01 10:15:00 - User logged out\n")
    fh.write("2024-06-01 10:20:00 - Application stopped\n")

with open("log.txt", "r") as fh:
    for line in fh:
        print(line.strip())  # Виводимо кожен рядок, видаляючи пробіли